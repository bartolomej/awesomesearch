<link rel="stylesheet" href="/metadata.css">

<div id="container">
    <img alt="{{title}}" src="{{image}}"/>
    <h1>{{title}}</h1>
    <a href="{{url}}">{{url}}</a>
    <p>{{description}}</p>
    <ul>
        {{#each tags}}
            <li>{{this}}</li>
        {{/each}}
    </ul>
</div>
